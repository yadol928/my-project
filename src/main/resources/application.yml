spring:
  application:
    name: projects
  datasource:
    url: jdbc:postgresql://localhost:5432/ProjectsAndTasks
    username: postgres
    password: 153698742
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true